name: pigs-flip7
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
    - streamlit>=1.28.0
    - pytest>=7.0.0
    - pytest-cov>=4.0.0
    - -e ..  # Install the flip7 package in editable mode
